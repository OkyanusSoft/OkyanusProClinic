/* ═══════════════════════════════════════════════════════════════════
   الحزمة 9 — إضافات src/store.tsx
   ربط جلسات المتابعة (العودات) بمحطة عمل الدكتور
   ═══════════════════════════════════════════════════════════════════

   ⚠️ ملاحظة مهمة: إن كان لديك حقل `fuId?: string` سابق في ClinicalSession
   (من ميزة «عودة للمتابعة» عند إنهاء الجلسة) فاتركه كما هو —
   الحقل الجديد اسمه fromFuId ولا يتعارض معه إطلاقاً.

   ─────────────────────────────────────────────────────────────────
   الخطوة 1 — واجهة ClinicalSession:
   ابحث عن:  export interface ClinicalSession {
   وأضف هذا السطر داخلها (مع بقية الحقول الاختيارية مثل invoiceId):
   ───────────────────────────────────────────────────────────────── */

  /** العودة التي فُتحت منها الجلسة (وضع المتابعة) */
  fromFuId?: string;

/* ─────────────────────────────────────────────────────────────────
   الخطوة 2 — اتحاد الأفعال (Action union):
   ابحث عن السطر:
     | { type: "START_SESSION"; patientId: string; doctorId: string; apptId?: string }
   واستبدله بهذا (أُضيف fromFuId):
   ───────────────────────────────────────────────────────────────── */

  | { type: "START_SESSION"; patientId: string; doctorId: string; apptId?: string; fromFuId?: string }

/* ─────────────────────────────────────────────────────────────────
   الخطوة 3 — حالة START_SESSION في الـ reducer:
   ابحث عن كائن الجلسة داخل الحالة (يحتوي السطر:  apptId: action.apptId, )
   وأضف بعده مباشرة هذا السطر:
   ───────────────────────────────────────────────────────────────── */

        apptId: action.apptId,
        fromFuId: action.fromFuId,   // ← أضف هذا السطر

/* ─────────────────────────────────────────────────────────────────
   الخطوة 4 — حالة END_SESSION في الـ reducer:
   أ) قبل الـ return الخاص بالحالة (بعد حساب sessions)، أضف:
   ───────────────────────────────────────────────────────────────── */

      // جلسة متابعة: أكمل العودة المرتبطة تلقائياً
      const returnFollowUps = s.fromFuId
        ? db.followUps.map((f) => (f.id === s.fromFuId ? { ...f, status: "done" as FollowUpStatus } : f))
        : db.followUps;

/* ─────────────────────────────────────────────────────────────────
   ب) في كائن الإرجاع return { ...db, ... } الخاص بـ END_SESSION،
      أضف هذا السطر مع بقية المفاتيح:
   ───────────────────────────────────────────────────────────────── */

        followUps: returnFollowUps,

/* ═══════════════════════════════════════════════════════════════════
   التحقق السريع بعد اللصق:
   - TypeScript يجب ألا يعطي أخطاء (FollowUpStatus معرّف مسبقاً في الملف)
   - جرّب: افتح جلسة من «قائمة العودات» → أنهِها → ستجد العودة
     تحولت إلى «مكتملة» في شاشة العودات والمتابعات
   ═══════════════════════════════════════════════════════════════════ */
