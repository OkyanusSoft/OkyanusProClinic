/* ═══════════════════════════════════════════════════════════════════
   الحزمة 9 — مقاطع دمج في src/pages/Session.tsx
   4 مقاطع قصيرة — نفّذها بالترتيب
   ═══════════════════════════════════════════════════════════════════

   ─────────────────────────────────────────────────────────────────
   المقطع 1 — الاستيراد:
   أضف هذا السطر مع بقية الاستيرادات أعلى الملف:
   ───────────────────────────────────────────────────────────────── */

import { PrevWorkReview, ReturnQueueSection } from "../components/ReturnQueue";

/* ─────────────────────────────────────────────────────────────────
   المقطع 2 — وسّع دالة start لتقبل معرّف العودة:
   ابحث عن:
     const start = (patientId: string, apptId?: string) => {
   واستبدل توقيعها بهذا:
     const start = (patientId: string, apptId?: string, fromFuId?: string) => {

   ثم داخلها ابحث عن:
     dispatch({ type: "START_SESSION", patientId, doctorId, apptId });
   واستبدله بهذا:
   ───────────────────────────────────────────────────────────────── */

    dispatch({ type: "START_SESSION", patientId, doctorId, apptId, fromFuId });

/* ─────────────────────────────────────────────────────────────────
   المقطع 3 — ضع قائمة العودات بجانب قائمة الانتظار:

   أ) ابحث عن قسم قائمة الانتظار — ستجد سطراً كهذا:
        <section className="anim-rise" style={{ animationDelay: "120ms" }}>
      (يليه عنوان «قائمة الانتظار — جاهزون للدخول»)
      ⬅ قبل هذا السطر مباشرة أضف:
   ───────────────────────────────────────────────────────────────── */

      <div className="grid xl:grid-cols-2 gap-5 items-start">

/* ─────────────────────────────────────────────────────────────────
   ب) ابحث عن قسم الجلسات المكتملة — سطر يحتوي:
        animationDelay: "220ms"
      ⬅ قبل الـ <section> الخاص به ستجد </section> يخص قائمة الانتظار.
      بعد ذلك الـ </section> مباشرة أضف:
   ───────────────────────────────────────────────────────────────── */

        {/* قائمة العودات — جاهزون للدخول */}
        <ReturnQueueSection onEnter={(pid, fuId) => start(pid, undefined, fuId)} />
      </div>

/* ─────────────────────────────────────────────────────────────────
   الشكل النهائي للمنطقة يجب أن يبدو هكذا:

      <div className="grid xl:grid-cols-2 gap-5 items-start">
        <section ...> قائمة الانتظار ... </section>
        <ReturnQueueSection onEnter={(pid, fuId) => start(pid, undefined, fuId)} />
      </div>

      <section ... animationDelay: "220ms" ...> جلسات اليوم المكتملة ...
   ───────────────────────────────────────────────────────────────── */

/* ─────────────────────────────────────────────────────────────────
   المقطع 4 — اعرض سجل الأعمال السابقة داخل الجلسة:
   ابحث عن شريط الجلسة العلوي — سطر يحتوي:
     bg-pine sidebar-texture text-white px-5 py-4
   ⬅ بعد انتهاء هذا الشريط (بعد الـ </div> الخاص به، وقبل شريط
     التبويبات) أضف هذا المقطع:
   ───────────────────────────────────────────────────────────────── */

      {/* وضع العودة — سجل الأعمال السابقة */}
      {session.fromFuId && (
        <PrevWorkReview patientId={session.patientId} fuId={session.fromFuId} />
      )}

/* ═══════════════════════════════════════════════════════════════════
   النتيجة:
   - في المحطة: «قائمة الانتظار» يساراً و«قائمة العودات» يميناً (شاشات واسعة)
   - الدخول من العودات يفتح الجلسة بلوحة كهرمانية تعرض كل الجلسات
     السابقة (إجراءات + أسنان + تشخيص + تقرير + فواتير + أدوية)
   - الطبيب يضيف العمل الجديد عبر التبويبات المعتادة ويحفظه مكمِّلاً
   - إنهاء الجلسة يعلّم العودة «مكتملة» تلقائياً
   ═══════════════════════════════════════════════════════════════════ */
