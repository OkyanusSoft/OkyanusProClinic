# سجل النسخ الاحتياطي — دفعة 2026-02-17 12:00 (دليل المستخدم)

## ملفات جديدة (لا تحتاج نسخاً)
- `src/pages/Guide.tsx` — شاشة دليل المستخدم الشاملة.
- إضافتان في `src/icons.tsx`: `IconBook` و`IconKey` (صادرات جديدة فقط — عكسها بحذف الدالتين).

## تعديلات جراحية في src/App.tsx (يمكن عكسها نقطةً نقطة)
النسخة الكاملة المرجعية قبل هذه الدفعة: `_backups/App-2026-02-15_18-40.tsx` + سلاسل الدفعات الموثقة اللاحقة.

1. **استيراد**: إضافة `IconBook` لقائمة الأيقونات، و`import GuidePage from "./pages/Guide";`.
2. **نوع Tab**: إضافة `"guide"` لاتحاد الأنواع.
3. **NAV**: إدخال `{ key: "guide", label: "دليل المستخدم", icon: <IconBook/> }` بعد «الإعدادات العامة».
4. **TITLES**: `guide: "دليل المستخدم"`.
5. **Shell**:
   - حالة جديدة `const [guideFocus, setGuideFocus] = useState<string | null>(null);`
   - تصفية القائمة: `NAV.filter((n) => n.key === "guide" || can(n.key))` — الدليل متاح لكل الأدوار دون صلاحية.
   - عرض: `{tab === "guide" && <GuidePage focus={guideFocus} />}`.
   - تمرير `onHelp={() => { setGuideFocus(tab); setTab("guide"); }}` للشريط العلوي.
6. **Topbar**: خاصية جديدة `onHelp` + زر كتاب قبل زر التنبيهات يفتح دليل الشاشة الحالية.
