# سجل النسخ الاحتياطي — دفعة 2026-02-17 10:00 (رسالة نجاح المزامنة عند الدخول)

## التعديل (جراحي وإضافي — قابل للعكس)
### src/App.tsx — مكوّن Shell
- **أضيف** تفكيك `conn` من `useStore()`.
- **أضيف** أثر `welcomeRef` بعد أثر حماية التبويب: عند أول دخول مستخدم (وبعد اكتمال فحص الاتصال) تُرسل إشعار نجاح «تمت مزامنة البيانات بنجاح» — مع وصف يوضح إن كانت القاعدة المركزية MySQL متصلة أم تم تحميل البيانات المحلية.
- **للاسترجاع:** احذف الأثر المضاف وسطر `conn` من التفكيك — أو استرجع `App.tsx` من آخر نسخة كاملة مبنية.

لا ملفات جديدة أو محذوفة في هذه الدفعة. البناء تحقق بنجاح بعد التعديل.
