# سجل النسخ الاحتياطي — دفعة 2026-02-19 10:00 (قائمة المتابعة في مراحل الجلسات)

## المشكلة
قائمة اختيار حالة المتابعة في عمود «المتابعة» بجدول مراحل الجلسات كانت تُقصّ وتختفي خلف المحتوى،
لأنها تفتح لأسفل (mt-2 + absolute) داخل حاوية الجدول المقيّدة بـ overflow-hidden / overflow-x-auto.

## الحل
- src/components/ui.tsx — تطوير مكوّن Drop:
  * خاصية `direction: "up" | "down"` (الافتراضي down للحفاظ على السلوك الحالي في بقية المواضع).
  * خاصية `fixed` — عند تفعيلها تُحسب إحداثيات اللوحة من getBoundingClientRect للزر وتُعرض بـ position: fixed،
    فتهرب من أي قصّ (clipping) سببه overflow في الأسلاف، وتفتح لأعلى أو لأسفل حسب المساحة المتاحة واتجاه direction.
  * إغلاق تلقائي عند التمرير/تغيير الحجم أثناء الفتح.
- src/pages/Session.tsx — عمود المتابعة: استخدام Drop بـ fixed و direction="up" + panelCls أوسع وأوضح.

## للاسترجاع
نسخة Drop الأصلية محفوظة في _backups/Users-2026-02-15_18-40.tsx ضمن ملف ui الأصلي (Drop بسيط بدون fixed/direction).
