# سجل النسخ الاحتياطي — دفعة 2026-02-16 15:00 (شاشة الإعدادات العامة + عنوان الفاتورة)

## نسخ كاملة
| الملف | النسخة |
|---|---|
| src/components/PrintSheet.tsx | `_backups/PrintSheet-2026-02-16_15-00.tsx` |
| (جديد) src/pages/Settings.tsx | لا يحتاج نسخة — ملف مستحدث |

## تعديلات جراحية موثقة

### src/store.tsx (السلسلة الأساسية: store-2026-02-15_18-40 + الدفعات اللاحقة)
1. بعد `CLINIC_LATIN`: واجهة `ClinicSettings` (اسم/لاتيني/عنوان/هاتف/بريد/بداية-نهاية الدوام/أيام تنبيه العودات/عنوان الفاتورة/بادئتها/تذييلها) + `DEFAULT_CLINIC_SETTINGS` + `clinicOf(db)`.
2. `PERMISSIONS`: إضافة `{ key: "settings", ... }` (للمدير افتراضياً).
3. واجهة `DB`: `settings: ClinicSettings`.
4. Action + reducer: `UPDATE_SETTINGS { patch }`.
5. البذور: `settings: DEFAULT_CLINIC_SETTINGS`؛ `load()`: دمج الافتراضي مع المحفوظ.
6. `END_SESSION`: رقم الفاتورة يستخدم `db.settings?.invoicePrefix`.

### ملفات أخرى
- **icons.tsx**: أيقونة `IconSettings` (ترس).
- **App.tsx**: تبويب "الإعدادات العامة" في NAV/TITLES/العرض + اسم العيادة في الشريط الجانبي والتذييل ديناميكي من `clinicOf(db)`.
- **Login.tsx / Reports.tsx**: اسم العيادة ديناميكي.
- **PrintSheet.tsx**: ترويسة الطباعة (الاسم/العنوان/الهاتف) + عنوان الفاتورة «فاتورة العيادة» + التذييل — كلها من الإعدادات.
- **Invoices.tsx**: بادئة رقم الفاتورة الجديدة من الإعدادات (رقم + عنوان النافذة).
- **Session.tsx**: `invNo` في `end()` يستخدم البادئة.
- **Appointments.tsx**: شبكة ساعات اليوم وخيارات وقت الحجز تُشتق من بداية/نهاية الدوام في الإعدادات.
