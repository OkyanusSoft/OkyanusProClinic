# سجل النسخ الاحتياطي — دفعة 2026-02-17 13:00 (أمثلة نموذج المريض بالهوية اليمنية)

## تعديل جراحى واحد — src/pages/Patients.tsx (نافذة إضافة مريض)
الأسطر قبل التعديل (للاسترجاع الدقيق):
1. سطر 275:
   `<TInput value={name} onChange={(e) => setName(e.target.value)} placeholder="مثال: سلمان عبدالعزيز الراشد" />`
   → أصبح: `placeholder="مثال: وائل عبدالله الشرفي"`
2. سطر 279:
   `<TInput value={phone} onChange={(e) => setPhone(e.target.value)} placeholder="05xxxxxxxx" dir="ltr" className="!text-start" />`
   → أصبح: `placeholder="77xxxxxxx"`
3. سطر 241 (التحقق — حدّث ليطابق الصيغة اليمنية وإلا رُفض المثال الجديد):
   `if (!/^05\d{8}$/.test(phone.trim())) return setErr("رقم الجوال يجب أن يكون بصيغة 05xxxxxxxx.");`
   → أصبح: يقبل 9 أرقام تبدأ بـ 7 (77/73/71/70/78…) مع رسالة خطأ محدّثة.
