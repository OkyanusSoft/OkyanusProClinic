# سجل النسخ الاحتياطي — دفعة 2026-02-19 13:00 (منظومة الطباعة والبطاقات)

## نسخ كاملة سابقة يُعتمد عليها
- أساس PrintSheet: مكوّنات الطباعة والبطاقات (PrintModal, CardPrintModal, CardSheet, PatientCardSheet, AppointmentCardPrint, FollowUpCardPrint, InvoicePrint, RxPrint)

## تعديلات هذه الدفعة
1. **كرت واحد لكل صفحة** (بطاقات المريض/الموعد/الرجوع):
   - `CardPrintModal`: إلغاء منتقي عدد النسخ [1,2,4] وتثبيت نسخة واحدة لكل صفحة A4.
   - `CardSheet`: إضافة معامل `scale` — البطاقة المفردة تُكبَّر وتُوسَّط على الصفحة.
   - `PatientCardSheet`: الوجه والظهر بحجم مكبّر متناسق على صفحة واحدة.
2. **الفاتورة**: إضافة البريد الإلكتروني للعيادة في الترويسة.
3. **قائمة الأسعار للطباعة**: الخدمات المفعّلة فقط (تُستبعد الموقوفة).
