# سجل النسخ الاحتياطي — دفعة 2026-02-16 12:00 (نظام العودات والمتابعة + مركز التقارير)

## نسخ كاملة
| الملف | النسخة |
|---|---|
| src/pages/Appointments.tsx | `_backups/Appointments-2026-02-16_12-00.tsx` |
| src/pages/Reports.tsx | `_backups/Reports-2026-02-16_12-00.tsx` |

## تعديلات جراحية موثقة (يمكن عكسها من هذا البيان)

### src/store.tsx (أساسه الكامل: `_backups/store-2026-02-15_18-40.tsx` + سلاسل الدفعات السابقة)
1. **أنواع جديدة** بعد `ClinicalSession`: `FollowUpStatus` ("pending"|"booked"|"done")، `FollowUp {id, patientId, doctorId, reason, dueDate, status, createdAt, apptId?, notes?}`، `FU_META` (بيانات الشارات)، `FOLLOWUP_SUGGESTIONS` (اقتراحات ذكية حسب العلاج)، `suggestFollowUp(names)`.
2. **واجهة DB**: إضافة `followUps: FollowUp[]`.
3. **البذور**: 7 عودات واقعية (متأخرة/اليوم/قادمة/محجوزة/مكتملة) وإضافتها لكائن الإرجاع.
4. **Action union**: ADD_FOLLOWUP, UPDATE_FOLLOWUP, DELETE_FOLLOWUP.
5. **Reducer**: الحالات الثلاث + تحسين `SET_APPT_STATUS` ليُكمل العودة المرتبطة تلقائياً عند إتمام موعدها.
6. **load()**: تطبيع `followUps: db.followUps ?? []`.

### src/pages/Session.tsx
- **شريط الخروج**: إضافة بطاقة «عودة للمتابعة» (مفتاح Switch + سبب مقترح تلقائياً حسب الإجراءات + تاريخ مستحق قابل للتعديل).
- **end()**: عند التفعيل يُرسَل `ADD_FOLLOWUP` مع إنهاء الجلسة، وتتحدث رسالة النجاح.
- **استيرادات مضافة**: `suggestFollowUp`, `uid`, `Switch`.

### src/pages/Patients.tsx (درج المريض)
- شبكة الإحصاءات `grid-cols-3` → `grid-cols-2 lg:grid-cols-4` مع بطاقة «العودة القادمة».
- قسم جديد «العودات المقررة» قبل سجل الزيارات مع زر «جدولة عودة».
- **استيرادات مضافة**: `FollowUpModal` من `./Appointments`، `FU_META`، `IconClock`.
