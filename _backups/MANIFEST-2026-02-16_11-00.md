# سجل النسخ الاحتياطي — دفعة 2026-02-16 11:00 (تكبير خريطة الأسنان وتجاوبها)

## نسخ كاملة
| الملف | النسخة |
|---|---|
| src/components/DentalChart.tsx | `_backups/DentalChart-2026-02-16_11-00.tsx` |

## تعديلات جراحية موثقة (بدون نسخ كاملة — يمكن عكسها يدوياً)

### src/pages/Session.tsx — تبويب «الجلسة والعلاج»
- **قبل:** جسم التبويب `grid lg:grid-cols-3` — الفحص والإجراءات في عمودين (col-span-2) وخريطة الأسنان محشورة في العمود الثالث الضيق.
- **بعد:** `space-y-5` — الفحص والإجراءات في `grid xl:grid-cols-2` جنباً إلى جنب، وقسم الخريطة **بعرض كامل** أسفلهما مع شارة «تغيير معلّق» موسعة وتمرير `editorNote` يوضح أن التثبيت يتم عند إنهاء الجلسة.
- للاسترجاع: النسخة الكاملة السابقة للتبويبات كاملة في `_backups/Session-2026-02-16_09-30.tsx`.

### src/index.css — قاعدة .tooth-btn
- **قبل:** `transform: scale(1.12); filter: brightness(1.05)` عند التحويم (بلا transform-box — يسبب انزياحاً في SVG).
- **بعد:** `transform-box: fill-box; transform-origin: center` مع تكبير لطيف `scale(1.05)`.
