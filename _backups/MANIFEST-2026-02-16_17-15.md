# سجل النسخ الاحتياطي — دفعة 2026-02-16 17:15 (إخفاء زر إعادة التعيين من القائمة)

## الملف: src/App.tsx
تغيير **حذفي بحت** (لا إضافة أسطر) — يمكن عكسه بإعادة إدراج المقاطع التالية:

1. في `SidebarContent`: حُذف الزر
```tsx
<button
  onClick={onReset}
  className="w-full text-center text-[11px] font-bold text-white/40 hover:text-white/80 py-2 rounded-lg hover:bg-white/5 cursor-pointer transition-colors"
>
  إعادة تعيين البيانات التجريبية
</button>
```
2. حُذف `onReset` من توقيع المكوّن (`onReset: () => void;` ومن قائمة التفكيك).
3. حُذف خاصّيتا `onReset={...}` من استدعائي `SidebarContent` في `Shell` (سطح المكتب والجوال).

**الوظيفة ما تزال متاحة** في: الإعدادات العامة ← البيانات والنسخ ← منطقة الخطر ← إعادة تعيين البيانات التجريبية (مع نافذة تأكيد).

للاسترجاع الكامل للحالة السابقة: النسخة الكاملة الأحدث للشريط الجانبي ضمن `_backups/App-2026-02-15_18-40.tsx` مع سلاسل الدفعات اللاحقة الموثقة في manifests 02-16.
