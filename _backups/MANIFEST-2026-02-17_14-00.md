# سجل النسخ الاحتياطي — دفعة 2026-02-17 14:00 (أزرار الطباعة الاحترافية)

## الإضافات (جديدة بالكامل — لا حذف لأي كود سابق)
1. **src/components/PrintSheet.tsx** — أضيف في نهاية الملف:
   - `MiniOdontogram`: خريطة أسنان مصغرة للطباعة (4 صفوف FDI بأسنان تاج+جذور ملونة بالحالة).
   - `PatientPrint({ patientId })`: ملف المريض المطبوع — هوية، حساسية، مخطط الأسنان، مؤشر الصحة، الزيارات، الروشتات، العودات، المستحقات.
   - `AppointmentsDayPrint({ date })`: كشف مواعيد اليوم المطبوع — جدول المواعيد + المتابعات المستحقة ضمن نافذة التنبيه من الإعدادات.
   - تحديث سطر استيراد store بإضافة: APPT_META, TOOTH_META, today, useAuth, type ToothStatus.
2. **src/pages/Patients.tsx** —
   - سطر 22: `PrintModal, RxPrint` ← أضيف `PatientPrint`.
   - خلية إجراءات الجدول: زر طابعة قبل TwoStepDelete + حالة `printId` + نافذة طباعة أسفل الجدول.
   - تذييل درج المريض: زر «طباعة الملف» + حالة `showPrint` + نافذة طباعة.
3. **src/pages/Appointments.tsx** —
   - استيراد `IconPrinter` و`PrintModal, AppointmentsDayPrint`.
   - `ScheduleView`: حالة `printDay` + صف «طباعة كشف اليوم» فوق شريط الأيام + نافذة الطباعة نهاية المكوّن.

## للاسترجاع
احذف الإضافات المذكورة أعلاه (كلها كتل جديدة مستقلة) وأعد سطور الاستيراد لأصلها.
