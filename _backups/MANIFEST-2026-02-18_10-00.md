# سجل النسخ الاحتياطي — دفعة 2026-02-18 10:00 (فئات المصروفات + ترقية مزامنة MySQL + وضوح القائمة)

## الملفات المعدّلة
1. **src/store.tsx** (جراحياً):
   - واجهة DB: إضافة `expenseCats: string[]`.
   - PERMISSIONS: مفتاح `expenseCats` + إدخاله في افتراضيات السكرتارية.
   - البذور: `expenseCats` من أسماء EXPENSE_CATS + إضافتها للإرجاع والتطبيع.
   - أفعال: ADD/RENAME/DELETE_EXPENSE_CAT (الحذف يعيد توزيع المصروفات على فئة بديلة).
   - `expCatColor`: ألوان احتياطية ثابتة بالهاش للفئات الجديدة/المعاد تسميتها.
2. **src/pages/ExpenseCats.tsx**: شاشة جديدة (إضافة/إعادة تسمية/حذف + إحصاءات وحصة كل فئة).
3. **src/App.tsx**:
   - تبويب `expenseCats` في مجموعة المالية بعد العملات + TITLES + عرض.
   - ترقية عناوين أقسام القائمة الجانبية (وزن أسود + شارة أيقونة + خط فاصل متدرج) وتوضيح ترويسة «القائمة الرئيسية».
4. **server/index.js**:
   - `ensureSchema()` — إنشاء الجداول تلقائياً عند التشغيل (لا حاجة لإعادة تنفيذ SQL).
   - `syncNormalized` يشمل الآن: supplies, supply_moves, service_cats, item_cats, expense_cats, sessions.
5. **server/schema.sql**: إضافة جداول الفئات والمخزون والجلسات.
