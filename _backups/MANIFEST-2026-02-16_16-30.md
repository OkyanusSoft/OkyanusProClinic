# سجل النسخ الاحتياطي — دفعة 2026-02-16 16:30 (الكمال: مخزون، خطط علاج، خصومات، عدم حضور، رسائل تذكير)

## سياسة هذه الدفعة
الملفات المعدّلة جراحياً لديها نسخ كاملة من دفعات سابقة (سلسلة موثقة). الجديد فقط:
- `src/pages/Inventory.tsx` — ملف جديد (لا نسخة سابقة).

## تعديلات موثقة (يمكن عكسها من هذا البيان)

### src/store.tsx (أساسه: store-2026-02-15_18-40 + سلاسل 18-40/09-30/12-00)
1. `ApptStatus` += "noshow" وإدخاله في `APPT_META` (label «لم يحضر», dot "#7d8ba0", cls "bg-mist text-soft").
2. `Invoice` += `discount?: number; method?: string` — و`invoiceTotal` يخصم نسبة الخصم.
3. `PAY_METHODS` ثابت (cash/card/transfer).
4. أنواع جديدة: `SupplyItem`, `SupplyMove`, `PlanItem`, `TreatmentPlan`.
5. واجهة DB += `supplies`, `supplyMoves`, `plans`.
6. بذور: 12 صنفاً طبياً (3 منها دون الحد الأدنى)، 14 حركة مخزون، خطتا علاج (ابتسامة نورة + تأهيل فهد).
7. Action union += ADD_SUPPLY, MOVE_SUPPLY, DELETE_SUPPLY, ADD_PLAN, UPDATE_PLAN, DELETE_PLAN.
8. Reducer: الحالات الست + تسجيل الحركة وخصم/زيادة الكمية بحد أدنى صفر.
9. load(): تطبيع المصفوفات الثلاث الجديدة.

### src/App.tsx
- NAV/TITLES/Tab += "inventory" (المخزون والمستهلكات، أيقونة IconBox) + شارة عدد الأصناف المنخفضة في القائمة الجانبية + عرض الصفحة.

### src/pages/Invoices.tsx
- نافذة فاتورة جديدة: حقل خصم % + اختيار طريقة الدفع.
- الجدول: شريحة خصم وشريحة طريقة دفع.
- الطباعة (PrintSheet InvoicePrint): سطر الخصم + طريقة الدفع.

### src/pages/Appointments.tsx
- زر «رسالة تذكير» في بطاقة الموعد → نافذة قوالب (تذكير مسبق/يوم الموعد/بعد الجلسة/عودات) مع تعويض متغيرات (الاسم، التاريخ، الوقت، الخدمة، الطبيب) ونسخ للحافظة.
- حالة «لم يحضر» متاحة من قائمة حالات الموعد (تلقائي عبر APPT_META).

### src/pages/Patients.tsx
- قسم «خطط العلاج» في درج المريض: شريط تقدم لكل خطة، إتمام بنود بنقرة، ونافذة إنشاء خطة ببنود ديناميكية (اسم/سن/تكلفة).
