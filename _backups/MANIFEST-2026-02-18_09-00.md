# سجل النسخ الاحتياطي — دفعة 2026-02-18 09:00 (شاشة التفضيلات + المظهر الداكن + الألوان المميزة)

## ملفات جديدة
- `src/prefs.ts` — وحدة التفضيلات (تحميل/حفظ/تطبيق + arLocale)
- `src/pages/Preferences.tsx` — شاشة التفضيلات

## تعديلات جراحية موثقة (للعكس)
1. **src/icons.tsx**: أيقونات IconSun، IconMoon، IconSliders.
2. **src/index.css**: متغير `--color-card`، كتلة `.dark` (mist/ink/soft/line/card)، قاعدة `.dark .bg-white` و`.dark .from-white`، كتلة `.reduce-motion`، تحويل `.card` و`.input` وhover الأزرار إلى متغيرات.
3. **src/main.tsx**: `applyPrefs(loadPrefs())` قبل العرض.
4. **src/store.tsx**: `const AR` → دالة `arLocale()` من prefs (fmtDate/fmtDateFull/dayName/monthName).
5. **src/components/ui.tsx**: مدة التوست من `loadPrefs().toastDur` + مدة حركة الشريط ديناميكية.
6. **src/App.tsx**: تبويب `prefs` + زر التفضيلات تحت «المستخدمون والصلاحيات» + الشاشة الافتراضية عند الدخول + ساعة الشريط العلوي بـ arLocale.
7. **src/pages/Reports.tsx** و**src/pages/Guide.tsx**: `ar-EG-u-nu-latn` → `arLocale()`.
