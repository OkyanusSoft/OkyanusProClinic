# سجل النسخ الاحتياطي — دفعة 2026-08-29 07:30
## القوائم الديناميكية: مدن + تخصصات + مسميات وظيفية بإضافة فورية

## التعديلات (للاسترجاع)
1. **src/store.tsx**:
   - واجهة DB: إضافة `cities: string[]` و`specialties: string[]` و`staffRoles: string[]`
   - Action union + reducer: `ADD_CITY` / `ADD_SPECIALTY` / `ADD_STAFF_ROLE` (حماية من التكرار، trim)
   - بذور: `cities: [...YEMEN_CITIES]`، `specialties: [...DEFAULT_SPECIALTIES]` (انتقلت من Team.tsx)، `staffRoles: [...STAFF_ROLES]`
   - تطبيع load(): القوائم الثلاث مع تراجع للبذور
2. **src/components/ui.tsx**: مكوّن جديد `SmartCombo` — حقل ذكي باقتراحات مُصفّاة + زر «إضافة للقائمة» عند كتابة قيمة غير موجودة + إغلاق بنقرة خارجية + لوحة متحركة.
3. **src/pages/Patients.tsx**: حقل المدينة في نافذة المريض ← SmartCombo مدعوم بـ db.cities + ADD_CITY + توست.
4. **src/pages/Team.tsx**: حقل التخصص في نافذة الطبيب ← SmartCombo بـ db.specialties، وحقل المسمى في نافذة الموظف ← SmartCombo بـ db.staffRoles.

## ملاحظة
- `SPECIALTIES` المحلية في Team.tsx أصبحت `DEFAULT_SPECIALTIES` المصدّرة من store.tsx.
- الإضافة فورية: القائمة المحدّثة تنعكس لحظياً في الحقل نفسه وكل الحقول الأخرى (مصدر حي واحد = db).
