# سجل النسخ الاحتياطي — دفعة 2026-02-16 09:30 (تطوير محطة العمل والتبويبات)

## نسخ كاملة في هذه الدفعة
| الملف | النسخة |
|---|---|
| src/components/DentalChart.tsx | `_backups/DentalChart-2026-02-16_09-30.tsx` |
| src/pages/Session.tsx | `_backups/Session-2026-02-16_09-30.tsx` |

## سلسلة استرجاع store.tsx
النسخة الكاملة الأساسية: `_backups/store-2026-02-15_18-40.tsx`
التعديلات اللاحقة (جراحية وناجحة، مرتبة زمنياً):

### دفعة صلاحيات النطاق (02-15 بعد 18:40)
1. إضافة عنصرين لـ `PERMISSIONS`: `scope_all_patients` و`scope_all_appointments` (خاصية `scope: true`).
2. إضافة الصلاحيتين لـ `defaults` دورَي secretary و assistant.
3. `AuthCtx`: إضافة `apptScope: string | null`، وحساب `hasAllPatients/hasAllAppts` و`patientScope/apptScope` في `AuthProvider`.
4. `load()`: ترقية صلاحيات السكرتارية/المساعدين القدماء بالصلاحيتين.

### دفعة التبويبات (02-16)
5. أنواع جديدة: `Implant`, `Prosthetic`, `OrthoCase`, `XrayRec` + ثوابت الخيارات + `DRUG_WATCH`.
6. إضافة المصفوفات الأربع لواجهة `DB`.
7. بذور: 3 زراعة، 4 تركيبات، 1 تقويم، 4 أشعة.
8. أفعال وإضافات reducer: ADD/DELETE للزراعة والتركيبات والأشعة، ADD/UPDATE/DELETE للتقويم.
9. `load()`: تطبيع المصفوفات الأربع الجديدة (`?? []`).

**طريقة الاسترجاع الكامل:** انسخ `_backups/store-2026-02-15_18-40.tsx` إلى `src/store.tsx` ثم أعد تطبيق النقاط 1–9 أعلاه، أو استرجع آخر حالة مبنية ناجحة من git/المحرر.
