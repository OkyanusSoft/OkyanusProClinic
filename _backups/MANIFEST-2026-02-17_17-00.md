# سجل النسخ الاحتياطي — دفعة 2026-02-17 17:00 (تقرير المرضى بأرقامهم في مركز التقارير)

## نسخة كاملة سابقة يُعتمد عليها
- `Reports-2026-02-16_12-00.tsx` (أساس مركز التقارير)

## تعديلات هذه الدفعة على src/pages/Reports.tsx (موثقة للعكس)
1. **سطر 35** — نوع التبويب:
   `type Tab = "overview" | "financial" | "doctors" | "followups" | "services" | "expenses";`
   → أُضيف `"patients"` في النهاية.
2. **سطر 60** — تفكيك useStore: أُضيف `patientBalance, lastVisit`.
3. **مصفوفة TABS** (بعد بند overview): أُضيف
   `{ key: "patients", label: "المرضى", icon: (c) => <IconUsers className={c} /> },`
4. **exportCurrent**: أُضيف فرع `tab === "patients"` يصدّر CSV بأعمدة
   (الاسم، الجوال، العمر، الجنس، المدينة، فصيلة الدم، الحساسية، التسجيل، آخر زيارة، زيارات، تسوس، معالج، مستحقات).
5. **جسم التقرير**: أُضيف قسم `tab === "patients"` — شريط KPI (إجمالي/جدد/لديهم تسوس/عليهم مستحقات) + جدول شامل بأرقام الجوال.

لا تعديلات على ملفات أخرى. البناء سليم بعد التنفيذ.
