# سجل النسخ الاحتياطي — دفعة 2026-02-17 15:00 (منظومة البطاقات + توحيد الطباعة A4)

## نسخ كاملة سابقة يُعتمد عليها للاسترجاع
- `PrintSheet-2026-02-16_12-00.tsx` (أساس مكوّن الطباعة)
- `Appointments-2026-02-16_12-00.tsx` و`Patients-2026-02-16_12-00.tsx`

## تعديلات هذه الدفعة (موثقة للعكس)
1. **src/index.css**: استبدال كتلة `@media print` القديمة (نهج visibility) بنظام `@page { size: A4; margin: 10mm }` + قاعدة `.print-only` (بوابة طباعة خارج #root) + أصناف البطاقات: `.print-a4` (ورقة 190مم)، `.dental-card` (85.6×54مم)، `.cut-box` (إطار قص متقطع).
2. **src/icons.tsx**: أيقونة جديدة `IconIdCard`.
3. **src/components/PrintSheet.tsx**:
   - `PrintPortal` — createPortal إلى document.body يُخفى على الشاشة ويطبع وحده.
   - `PrintModal` يرسل نسخة الطباعة عبر البوابة (عرض max-w-3xl + تمرير أفقي للجوال).
   - `CardPrintModal` — محدد «عدد البطاقات في الورقة» (1/2/4) + زر طباعة.
   - قوالب: `PatientCardPrint` (وجه + ظهر مع باركود وشريط حساسية)، `AppointmentCardPrint`، `FollowUpCardPrint`، `CardSheet` (شبكة مع خطوط قص).
4. **src/pages/Patients.tsx**: زر كرت في صف الجدول + زر «كرت المريض» في درج الملف + نافذة `CardPrintModal`.
5. **src/pages/Appointments.tsx**: زر كرت موعد في بطاقة الجدول اليومي + زر كرت رجوع في تبويب العودات + نوافذ الطباعة.
