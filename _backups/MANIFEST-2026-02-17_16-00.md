# سجل النسخ الاحتياطي — دفعة 2026-02-17 16:00 (إصلاح الطباعة الفارغة)

## سبب العطل
قاعدة `@media print { #root { display: none } .print-only { display: block } }` كانت عامة،
فأي طباعة خارج نوافذ المعاينة (التقارير، Ctrl+P) تُخفي التطبيق كله وتُخرج ورقة فارغة.

## الإصلاح (للعكس)
1. **src/index.css**:
   - حُصرت قواعد بوابة A4 تحت `body.print-doc` فقط.
   - أُضيفت قواعد طباعة عامة لكل الشاشات: إخفاء `aside`/`header`/`footer` وأزرار الواجهة عند الطباعة،
     خلفية بيضاء، و`print-color-adjust: exact` لحفظ الألوان.
2. **src/components/PrintSheet.tsx**:
   - `usePrintTrigger()` — يضيف `print-doc` لجسم الصفحة عند فتح النافذة،
     وعند الضغط على «طباعة الآن» يُخفي `#root` ويُظهر الورقة بأنماط inline مباشرة (لا يعتمد على استجابة المتصفح لـ CSS)،
     ثم يعيد كل شيء بعد انتهاء الطباعة (حدث afterprint + مهلة احتياطية).
   - `PrintModal` و`CardPrintModal` يستخدمانه بدل `window.print()` المجرد.
